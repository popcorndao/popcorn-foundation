import { Timer as Icon } from "@material-ui/icons";
import styled from "styled-components";
import { baseStyle } from "./baseStyle";

export const FocusTimerIcon = styled(Icon)`
  ${baseStyle}
`;

export default FocusTimerIcon;
