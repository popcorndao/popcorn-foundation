import { Twitter  as Icon } from "@material-ui/icons";
import styled from "styled-components";
import { baseStyle } from "./baseStyle";

export const TwitterIcon = styled(Icon)`
  ${baseStyle}
`;

export default TwitterIcon;